name: Deploy to Firebase

# Auto-deploy เมื่อ push เข้า main (กดรันเองได้ผ่าน workflow_dispatch)
# build static (pnpm) → deploy hosting ขึ้น Firebase
# หมายเหตุ: deploy เฉพาะ hosting — functions/rules deploy มือ
#   (firebase deploy --only functions,firestore:rules,storage)
#
# ── Secrets ที่ต้องตั้งใน repo (Settings → Secrets and variables → Actions) ──
#   FIREBASE_SERVICE_ACCOUNT : เนื้อ JSON ของ service account (ทั้งไฟล์)
#       (สร้างโดย `firebase init hosting:github`)
#   FIREBASE_PROJECT_ID      : project id ของ tenant นี้ เช่น livetubex-admin
#   NEXT_PUBLIC_FIREBASE_API_KEY
#   NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
#   NEXT_PUBLIC_FIREBASE_PROJECT_ID
#   NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
#   NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
#   NEXT_PUBLIC_FIREBASE_APP_ID
#   NEXT_PUBLIC_LINE_LIFF_ID
#   NEXT_PUBLIC_BOOTSTRAP_OWNER_EMAIL
# (NEXT_PUBLIC_* ต้องมีตอน build เพราะ static export ฝัง config ลง bundle)
#
# ── Whitelabel ────────────────────────────────────────────────────────────────
# 1 บริษัท = 1 Firebase project = 1 ชุด secrets ในรีโปที่ deploy ให้เจ้านั้น
# (แยกรีโป หรือใช้ GitHub Environments ต่อ tenant ก็ได้ — เปลี่ยนแค่ค่า secrets)
# ชื่อระบบ/โลโก้/สี ไม่ได้อยู่ใน secrets — อยู่ใน Firestore publicSettings/brand

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      # ── Frontend: static export → out/ ──
      - name: Install frontend deps
        run: pnpm install --frozen-lockfile

      - name: Build static site
        run: pnpm run build
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
          NEXT_PUBLIC_LINE_LIFF_ID: ${{ secrets.NEXT_PUBLIC_LINE_LIFF_ID }}
          NEXT_PUBLIC_BOOTSTRAP_OWNER_EMAIL: ${{ secrets.NEXT_PUBLIC_BOOTSTRAP_OWNER_EMAIL }}

      # ── Deploy hosting ──
      # ใช้ w9jds/firebase-action (รันใน Docker, runtime ของตัวเอง) เลี่ยง bug
      # HTTP client ของ firebase-tools บน Node ของ runner ("Premature close")
      - name: Deploy to Firebase
        uses: w9jds/firebase-action@v15.22.3
        with:
          # hosting อย่างเดียว: SA จาก firebase init มีสิทธิ์นี้อยู่แล้ว
          # functions/rules ต้องการ IAM roles เพิ่ม — deploy มือ หรือเพิ่มสิทธิ์ทีหลัง
          args: deploy --only hosting --force
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
